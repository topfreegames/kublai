language: node

node:
  - 6.2

sudo: required

services:
  - postgresql
  - redis

addons:
  postgresql: "9.4"

before_install:
  - nvm install 6.2.2
  - nvm use 6.2.2

install:
  - make setup-ci

script:
  - env
  - node --version
  - make test-ci
